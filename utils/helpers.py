# This file can contain utility functions or classes
# that may be reused across cogs.

def format_text_for_embed(text: str) -> str:
    """Example helper function that might format text for an embed."""
    return f"**{text}**"
